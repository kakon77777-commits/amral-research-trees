# dependencies/

Upstream package artifacts this package's claims build on — see each file's own provenance before trusting it as independently produced here.

Auto-generated directory index (no directory in this repository is left without a README — see `RESEARCH-TREE-PROTOCOL.md` at the repo root). Path: `dependencies`.

**Files:**
- `v1.0_finite_layer_augmentation_certificate.json`
- `v1.0_zip.sha256.txt`

For what this content claims, its epistemic status, and how to read it in context, see the nearest ancestor README with prose (this package's own root `README.md`, or the tree root's `README.md`).
