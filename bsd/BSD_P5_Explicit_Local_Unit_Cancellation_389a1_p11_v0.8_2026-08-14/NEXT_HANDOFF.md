# NEXT HANDOFF — BSD P5 v0.8

Do not re-run the $p=11$ Kurihara/Selmer, IMC, Bockstein-nonvanishing, or local logarithm calculations.

Current exact local certificate for $E=389.a1$, $p=11$:

$$
\#E(\mathbb F_{11})=16,
\qquad
a_{11}=-4.
$$

With $P=(0,0)$ and the minimal Neron differential,

$$
v_{11}(\log_{\omega}P)=1,
\qquad
\frac{\log_{\omega}P}{11}\equiv4\pmod{11}.
$$

For $S_0=\{\infty,11,389\}$,

$$
u_{\mathrm{loc}}
=
\frac{16}{11}\frac{388}{389}\log_{\omega}(P)
\in\mathbb Z_{11}^{\times},
$$

and

$$
u_{\mathrm{loc}}\equiv4\pmod{11}.
$$


Thus, after the inherited BKS line identifications, the remaining coefficient is reduced to

$$
\beta_{\xi}
=
\frac{L^{*}(E,1)}{\Omega_{\xi}R_{\infty}}.
$$

Priority target:

$$
\mathrm{P5\!-DESC}^{\mathrm{red}}_{11}:
\quad
\beta_{\xi}\in\mathbb Q_{11}.
$$

Then prove only

$$
v_{11}(\beta_{\xi})=0.
$$

Do not use numerical closeness of the BSD ratio to $1$ as proof. Do not use BKS GPR(ii) from $\mathrm{BSD}_{11}(E)$ because that is circular for this project.
