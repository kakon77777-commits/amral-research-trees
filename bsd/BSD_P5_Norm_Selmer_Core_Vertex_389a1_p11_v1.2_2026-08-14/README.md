# BSD P5 v1.2 package

This package upgrades v1.1 from a local norm-localization determinant to an exact modified-Selmer cube.

Under the inherited theorem-chain fact

$$
\Sha(389.a1/\mathbb Q)[11]=0,
$$

one obtains

$$
\operatorname{Sel}_{11}(E/\mathbb Q)\simeq\mathbb F_{11}^2.
$$

The norm local conditions at $397$ and $991$ give

$$
\dim=2,1,1,0,
$$

with

$$
K_{397}=\mathbb F_{11}(Q-2P),
\qquad
K_{991}=\mathbb F_{11}(Q-4P),
$$

and

$$
(Q-2P)\wedge(Q-4P)=2(P\wedge Q).
$$

Thus the finite rank-$2$ arithmetic core is primitive and completely detected by the two anomalous Kolyvagin directions.

The comparison with the v1.0 Mazur--Tate class remains open and is isolated as

$$
\mathrm{P5\! - \!ANOM\! - \!BocCOMP}_{11}^{(2)}.
$$
