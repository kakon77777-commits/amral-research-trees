# Data

- `results.v1.json` is the compact machine-readable statement of exact certificates, finite domains, no-hit counts, and provenance.
- `public-4x6-packets.v1.json` snapshots all 71 public numeric packets used by the audit and records the digest definition used by the original script.
- `raw-logs/` contains the original process-stream captures. Use `../code/verify_run_logs.py` to select completed disjoint ranges and recompute the principal aggregates; do not sum every similarly named preliminary file indiscriminately.
