# engineering (AI-4)

Contains `AI-4_Phase0_工程章程.md` (the role's own charter) and `pnp-glc-i0/`, the
actual candidate implementation AI-4 builds and self-tests each round. `pnp-glc-i0/`
has its own README (AI-4's original project documentation) plus per-subdirectory
READMEs added alongside it explaining the versioned-snapshot pattern that accumulated
over rounds v0.2.1 through v0.2.6 -- see `pnp-glc-i0/README.md` first, then the
subdirectory you're actually in.
