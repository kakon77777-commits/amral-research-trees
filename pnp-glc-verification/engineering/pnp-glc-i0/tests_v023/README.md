# tests_v023/

Unit tests for `../src/pnp_glc_i0/semantic_validator_v023.py` (and, in later
rounds, the other `_v023` modules) -- the v0.2.3 candidate's versioned validator
logic specifically. Run analogously to `../tests/` but pointed at this directory.

This snapshot corresponds to candidate v0.2.3. The blocker(s) being tested/closed at this round: CLOSURE-EDGE-SCOPE-01 / ORACLE-DECL-FAMILY-01. For the full narrative, see `../CURRENT-v0.2.3-candidate.md` and `../VALIDATION-REPORT-v0.2.3-candidate.md` (filenames vary slightly by round -- check `../` for the exact match).
