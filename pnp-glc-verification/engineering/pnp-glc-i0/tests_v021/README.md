# tests_v021/

Unit tests for `../src/pnp_glc_i0/semantic_validator_v021.py` (and, in later
rounds, the other `_v021` modules) -- the v0.2.1 candidate's versioned validator
logic specifically. Run analogously to `../tests/` but pointed at this directory.

This snapshot corresponds to candidate v0.2.1. The blocker(s) being tested/closed at this round: REF-TYPE-01. For the full narrative, see `../CURRENT-v0.2.1-candidate.md` and `../VALIDATION-REPORT-v0.2.1-candidate.md` (filenames vary slightly by round -- check `../` for the exact match).
