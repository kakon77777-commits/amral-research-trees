# fixtures/ (live, unversioned)

Input problem instances (2-SAT cases, oracle declarations, adversarial/malformed
cases) used to generate the live `artifacts/` set. See `../README.md`'s "Fixtures"
table for what each fixture name is designed to exercise and its expected outcome.
