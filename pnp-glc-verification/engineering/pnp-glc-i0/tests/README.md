# tests/ (base)

Unit tests for the unversioned base implementation (`test_parity.py`,
`test_semantic_validator.py`, `test_two_sat.py`) -- run via
`python -m unittest discover -s tests -v` per `../README.md`'s "Reproduce" section.
Versioned successors are in `../tests_v0XX/`, one directory per candidate round.
